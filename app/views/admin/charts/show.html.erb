<% content_for(:title) do %>
  Dashboard: <%= params[:resource].humanize %> grouped by "<%= params[:group_attribute].humanize %>" and applied "<%= params[:function] %>" function on attribute "<%= params[:attribute_to_apply_function] %>"
<% end %>

<header class="main-content__header" role="banner">
  <h1 class="main-content__page-title">
    <%= content_for(:title) %>
  </h1>

  <div>
    <%= link_to t("administrate.actions.back"), admin_charts_path(permitted_chart_params(params)), class: "button" %>
  </div>
</header>

<section class="main-content__body">
  <%= draw_chart params %>
</section>
